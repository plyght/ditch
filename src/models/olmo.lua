return {
  model_type = "olmo",
  llama_cpp = "olmo",
  chat = "olmo",
  verified = true,
  notes = "fixture: non-parametric LayerNorm (the default norm names exist only as slots), clip_qkv.",
  norm = "none",
}
